class Solution {
public:
    vector<int> grayCode(int n) {
        vector<int>goal;
        for(int i=0;i<(1<<n);i++)
        {
            int x=i>>1;
            goal.push_back(x^i);
        }
        return goal;
    }
};
